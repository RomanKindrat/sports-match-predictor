# Export artifacts directly from notebook

Run from repo root:

```bash
backend/.venv/bin/python model/export_artifact_from_notebook.py
```

Artifacts will be created in:

- `model/artifacts/notebook_gb.pkl`
- `model/artifacts/notebook_gb_metadata.json`
